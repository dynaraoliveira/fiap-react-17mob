import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row',
        flexWrap: "wrap",
        justifyContent: "center",
    },
    button: {
      width: '80%',
      justifyContent: "center",
      margin: 10,
    }
});